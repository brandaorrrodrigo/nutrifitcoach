se dia atual tem ≥1 foto aprovada:
  if last_day = hoje-1  → current_streak += 1
  else                   → current_streak = 1
best_streak = max(best_streak, current_streak)
bonus_streak = floor(current_streak/3)  -- 1 ponto extra a cada 3 dias
